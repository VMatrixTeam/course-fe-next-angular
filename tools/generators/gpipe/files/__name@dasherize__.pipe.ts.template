import { Pipe, PipeTransform } from '@angular/core';

@Pipe({ pure: true, name: '<%= camelize(name) %>'})
export class <%= classify(name) %>Pipe implements PipeTransform {
  transform(value: any) {

  }
}
